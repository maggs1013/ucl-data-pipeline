import pandas as pd, os

def main():
    # Placeholder: merge injury/ref/GK data if you create those CSVs in /data.
    # For now this script just passes.
    print("Enrich step: no manual merges defined, skipping.")

if __name__=="__main__": main()
